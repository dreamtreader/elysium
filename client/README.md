Go to lib / DB.ts and change "baseURL" with your api endpoint (backend) domain url.

Clone the repo and run "npm run dev" in the command bar. Make sure your database is connected to the api endpoint.
